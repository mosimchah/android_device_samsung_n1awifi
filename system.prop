#
# system.prop for n1awifi
#

# Radio
ro.radio.noril=yes

# HW (this enables the navbar, a user can easily set it to 1 to disable it)
qemu.hw.mainkeys=0 

# Disable persistent GPU compositing
debug.sf.disable_hwc=1

# Fixes game grashing (HeartStone)
debug.egl.force_msaa=true
